/*
const effectContainer = document.querySelector("#effect");
const effects = [
    {
        name: "Smoke",
        image: "./includes/Smoke.gif"
    }
]

function addEffect(name, image){
    const effectDiv = document.createElement("div");
    effectDiv.classList.add("effect");
    effectContainer.appendChild(effectDiv);

    const effectName = document.createElement("div");
    effectName.innerText = name;
    effectContainer.appendChild(effectName);

    const effectImage = document.createElement("img");
    effectImage.src = image;
    effectContainer.appendChild(effectImage);
}

function addEffects(){
    for(let effect of effects){
        addEffect(effect.name, effect.image);
    }
}
*/

